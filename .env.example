# Demo login credentials (front-end mock auth only — NOT secure).
# Copy this file to `.env` to override the defaults (admin / admin123).
# Must be VITE_-prefixed to be readable by the browser bundle.
VITE_DEMO_USERNAME=admin
VITE_DEMO_PASSWORD=admin123
